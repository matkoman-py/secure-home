import  com.example.myhome.domain.*;
rule "1"
    when
        dto : DroolsDTO( deviceType=="door1" && (value >= 30.1  || value <=15.0));
    then
        dto.setAlarm(true);
end;
rule "2"
    when
        dto : DroolsDTO( deviceType=="door2" && (value >= 20.0  || value <=11.0));
    then
        dto.setAlarm(true);
end;
